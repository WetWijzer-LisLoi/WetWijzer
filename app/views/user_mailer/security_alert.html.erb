<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: linear-gradient(135deg, #dc2626, #b91c1c); color: white; padding: 30px; border-radius: 8px 8px 0 0; text-align: center; }
    .content { background: #fff; padding: 30px; border: 1px solid #e5e7eb; border-top: none; }
    .alert-box { background: #fef2f2; border: 1px solid #fecaca; border-radius: 6px; padding: 15px; margin: 20px 0; }
    .info-row { display: flex; padding: 8px 0; border-bottom: 1px solid #e5e7eb; }
    .info-label { font-weight: 600; width: 120px; color: #6b7280; }
    .btn { display: inline-block; background: #dc2626; color: white; padding: 12px 24px; text-decoration: none; border-radius: 6px; font-weight: 600; }
    .footer { text-align: center; padding: 20px; color: #6b7280; font-size: 14px; }
  </style>
</head>
<body>
  <div class="header">
    <h1>🔒 <%= t("mailer.security_alert.#{@event_type}.title") %></h1>
  </div>
  
  <div class="content">
    <p><%= t('mailer.security_alert.greeting', name: @user.name || @user.email) %></p>
    
    <div class="alert-box">
      <p><strong><%= t("mailer.security_alert.#{@event_type}.message") %></strong></p>
    </div>
    
    <div style="margin: 20px 0;">
      <div class="info-row">
        <span class="info-label"><%= t('mailer.security_alert.time') %></span>
        <span><%= Time.current.strftime('%d/%m/%Y %H:%M %Z') %></span>
      </div>
      <% if @ip_address %>
      <div class="info-row">
        <span class="info-label"><%= t('mailer.security_alert.ip_address') %></span>
        <span><%= @ip_address %></span>
      </div>
      <% end %>
      <% if @user_agent %>
      <div class="info-row">
        <span class="info-label"><%= t('mailer.security_alert.device') %></span>
        <span><%= @user_agent.truncate(60) %></span>
      </div>
      <% end %>
    </div>
    
    <p><%= t('mailer.security_alert.not_you') %></p>
    
    <p style="text-align: center; margin: 30px 0;">
      <a href="<%= account_url %>" class="btn"><%= t('mailer.security_alert.review_button') %></a>
    </p>
  </div>
  
  <div class="footer">
    <p>WetWijzer / LisLoi</p>
  </div>
</body>
</html>
