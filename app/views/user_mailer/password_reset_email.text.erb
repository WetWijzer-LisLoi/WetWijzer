<%= t('mailer.password_reset.greeting', name: @user.name || @user.email) %>

<%= t('mailer.password_reset.body') %>

<%= t('mailer.password_reset.button') %>: <%= @reset_url %>

<%= t('mailer.password_reset.expires') %>

<%= t('mailer.password_reset.ignore') %>
