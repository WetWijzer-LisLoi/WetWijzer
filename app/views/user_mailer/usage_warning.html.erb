<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: linear-gradient(135deg, #f59e0b, #d97706); color: white; padding: 30px; border-radius: 8px 8px 0 0; text-align: center; }
    .content { background: #fff; padding: 30px; border: 1px solid #e5e7eb; border-top: none; }
    .progress-bar { background: #e5e7eb; border-radius: 9999px; height: 20px; overflow: hidden; margin: 20px 0; }
    .progress-fill { background: linear-gradient(90deg, #f59e0b, #ef4444); height: 100%; border-radius: 9999px; }
    .btn { display: inline-block; background: #2563eb; color: white; padding: 12px 24px; text-decoration: none; border-radius: 6px; font-weight: 600; }
    .footer { text-align: center; padding: 20px; color: #6b7280; font-size: 14px; }
  </style>
</head>
<body>
  <div class="header">
    <h1>⚠️ <%= t('mailer.usage_warning.title') %></h1>
  </div>
  
  <div class="content">
    <p><%= t('mailer.usage_warning.greeting', name: @user.name || @user.email) %></p>
    
    <p><%= t('mailer.usage_warning.body', percent: @usage_percent) %></p>
    
    <div class="progress-bar">
      <div class="progress-fill" style="width: <%= @usage_percent %>%"></div>
    </div>
    <p style="text-align: center; font-weight: bold;"><%= @usage_percent %>% <%= t('mailer.usage_warning.used') %></p>
    
    <p><%= t('mailer.usage_warning.upgrade_prompt') %></p>
    
    <p style="text-align: center; margin: 30px 0;">
      <a href="<%= @upgrade_url %>" class="btn"><%= t('mailer.usage_warning.upgrade_button') %></a>
    </p>
  </div>
  
  <div class="footer">
    <p>WetWijzer / LisLoi</p>
  </div>
</body>
</html>
