<% @title = I18n.locale == :fr ? 'Recherche unifiée' : 'Unified zoeken' %>

<div class="max-w-6xl mx-auto">
  <div class="mb-6">
    <h1 class="text-3xl font-bold text-gray-900 dark:text-white mb-2">
      <% if I18n.locale == :fr %>
        🔍 Recherche unifiée
      <% else %>
        🔍 Unified zoeken
      <% end %>
    </h1>
    <p class="text-gray-600 dark:text-gray-400">
      <% if I18n.locale == :fr %>
        Recherchez dans la législation, la jurisprudence et les travaux parlementaires
      <% else %>
        Zoek in wetgeving, rechtspraak en parlementaire stukken
      <% end %>
    </p>
  </div>

  <!-- Search Form -->
  <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg p-6 mb-6">
    <%= form_with url: unified_search_path, method: :get, local: true do %>
      <!-- Search Input -->
      <div class="mb-4">
        <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-2">
          <% if I18n.locale == :fr %>Rechercher<% else %>Zoeken<% end %>
        </label>
        <div class="flex gap-3">
          <input type="text" name="q" value="<%= @query %>" 
                 class="flex-1 rounded-lg border-gray-300 dark:border-gray-600 dark:bg-gray-700 dark:text-white text-lg px-4 py-3"
                 placeholder="<%= I18n.locale == :fr ? 'Entrez votre recherche...' : 'Voer uw zoekopdracht in...' %>"
                 autofocus>
          <button type="submit" class="px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-medium rounded-lg">
            <% if I18n.locale == :fr %>Rechercher<% else %>Zoeken<% end %>
          </button>
        </div>
      </div>

      <!-- Source Checkboxes -->
      <div class="border-t border-gray-200 dark:border-gray-700 pt-4">
        <label class="block text-sm font-medium text-gray-700 dark:text-gray-300 mb-3">
          <% if I18n.locale == :fr %>Sources<% else %>Bronnen<% end %>
        </label>
        <div class="flex flex-wrap gap-4">
          <% @available_sources.each do |source| %>
            <label class="flex items-center gap-2 px-4 py-2 rounded-lg border cursor-pointer transition-colors
                          <%= @sources.include?(source[:key]) ? 'border-blue-500 bg-blue-50 dark:bg-blue-900/30' : 'border-gray-300 dark:border-gray-600 hover:border-gray-400' %>">
              <input type="checkbox" name="sources[]" value="<%= source[:key] %>"
                     <%= 'checked' if @sources.include?(source[:key]) %>
                     class="rounded border-gray-300 text-blue-600 focus:ring-blue-500">
              <% case source[:icon] %>
              <% when 'document' %>
                <svg class="w-5 h-5 text-gray-600 dark:text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
                </svg>
              <% when 'scale' %>
                <svg class="w-5 h-5 text-gray-600 dark:text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 6l3 1m0 0l-3 9a5.002 5.002 0 006.001 0M6 7l3 9M6 7l6-2m6 2l3-1m-3 1l-3 9a5.002 5.002 0 006.001 0M18 7l3 9m-3-9l-6-2m0-2v2m0 16V5m0 16H9m3 0h3"/>
                </svg>
              <% when 'building' %>
                <svg class="w-5 h-5 text-gray-600 dark:text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/>
                </svg>
              <% end %>
              <span class="text-sm font-medium text-gray-700 dark:text-gray-300"><%= source[:label] %></span>
            </label>
          <% end %>
        </div>
      </div>
    <% end %>
  </div>

  <!-- Results Summary -->
  <% if @query.present? %>
    <div class="mb-4 text-sm text-gray-600 dark:text-gray-400">
      <% if I18n.locale == :fr %>
        <%= @total_count %> résultats pour "<%= @query %>"
      <% else %>
        <%= @total_count %> resultaten voor "<%= @query %>"
      <% end %>
    </div>
  <% end %>

  <!-- Results by Source -->
  <% if @results.any? %>
    <div class="space-y-6">
      <% @results.each do |source, data| %>
        <% next if data[:items].empty? %>
        <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg overflow-hidden">
          <div class="px-4 py-3 bg-gray-50 dark:bg-gray-700 border-b border-gray-200 dark:border-gray-600">
            <div class="flex items-center justify-between">
              <h2 class="font-semibold text-gray-900 dark:text-white flex items-center gap-2">
                <% case source %>
                <% when :legislation %>
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
                  </svg>
                  <%= I18n.locale == :fr ? 'Législation' : 'Wetgeving' %>
                <% when :jurisprudence %>
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 6l3 1m0 0l-3 9a5.002 5.002 0 006.001 0M6 7l3 9M6 7l6-2m6 2l3-1m-3 1l-3 9a5.002 5.002 0 006.001 0M18 7l3 9m-3-9l-6-2m0-2v2m0 16V5m0 16H9m3 0h3"/>
                  </svg>
                  <%= I18n.locale == :fr ? 'Jurisprudence' : 'Rechtspraak' %>
                <% when :parliamentary %>
                  <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/>
                  </svg>
                  <%= I18n.locale == :fr ? 'Travaux parlementaires' : 'Parlementaire stukken' %>
                <% end %>
              </h2>
              <span class="text-sm text-gray-500 dark:text-gray-400">
                <%= data[:total] %> <%= I18n.locale == :fr ? 'résultats' : 'resultaten' %>
              </span>
            </div>
          </div>
          
          <div class="divide-y divide-gray-200 dark:divide-gray-700">
            <% data[:items].each do |item| %>
              <a href="<%= item[:url] %>" class="block p-4 hover:bg-gray-50 dark:hover:bg-gray-700 transition-colors">
                <h3 class="font-medium text-gray-900 dark:text-white">
                  <%= item[:title] %>
                </h3>
                <p class="text-sm text-gray-500 dark:text-gray-400 mt-1">
                  <%= item[:subtitle] %>
                </p>
              </a>
            <% end %>
          </div>
        </div>
      <% end %>
    </div>
  <% elsif @query.present? %>
    <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg p-8 text-center">
      <p class="text-gray-500 dark:text-gray-400">
        <% if I18n.locale == :fr %>
          Aucun résultat trouvé. Essayez d'autres termes ou sélectionnez plus de sources.
        <% else %>
          Geen resultaten gevonden. Probeer andere termen of selecteer meer bronnen.
        <% end %>
      </p>
    </div>
  <% end %>

  <!-- Link to chatbot -->
  <div class="mt-8 p-4 bg-blue-50 dark:bg-blue-900/30 rounded-xl">
    <div class="flex items-center gap-4">
      <div class="flex-shrink-0">
        <svg class="w-8 h-8 text-blue-600 dark:text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 10h.01M12 10h.01M16 10h.01M9 16H5a2 2 0 01-2-2V6a2 2 0 012-2h14a2 2 0 012 2v8a2 2 0 01-2 2h-5l-5 5v-5z"/>
        </svg>
      </div>
      <div class="flex-1">
        <h3 class="font-medium text-gray-900 dark:text-white">
          <% if I18n.locale == :fr %>
            Besoin d'aide pour comprendre?
          <% else %>
            Hulp nodig bij het begrijpen?
          <% end %>
        </h3>
        <p class="text-sm text-gray-600 dark:text-gray-400">
          <% if I18n.locale == :fr %>
            Posez vos questions à notre assistant IA juridique
          <% else %>
            Stel uw vragen aan onze juridische AI-assistent
          <% end %>
        </p>
      </div>
      <a href="<%= chatbot_path %>" class="px-4 py-2 bg-blue-600 hover:bg-blue-700 text-white font-medium rounded-lg">
        <% if I18n.locale == :fr %>Ouvrir le chatbot<% else %>Open chatbot<% end %>
      </a>
    </div>
  </div>
</div>
