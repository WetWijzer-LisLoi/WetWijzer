<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <style>
    body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #333; max-width: 600px; margin: 0 auto; padding: 20px; }
    .header { background: linear-gradient(135deg, #2563eb, #1d4ed8); color: white; padding: 24px; border-radius: 8px 8px 0 0; text-align: center; }
    .content { background: #f9fafb; padding: 24px; border: 1px solid #e5e7eb; border-top: none; }
    .button { display: inline-block; background: #2563eb; color: white; padding: 12px 24px; text-decoration: none; border-radius: 6px; font-weight: 500; }
    .footer { margin-top: 24px; padding-top: 16px; border-top: 1px solid #e5e7eb; font-size: 12px; color: #6b7280; }
    .query-box { background: white; border: 1px solid #e5e7eb; border-radius: 6px; padding: 12px 16px; margin: 16px 0; }
  </style>
</head>
<body>
  <div class="header">
    <h1 style="margin: 0; font-size: 24px;"><%= I18n.locale == :fr ? 'LisLoi' : 'WetWijzer' %></h1>
  </div>
  
  <div class="content">
    <h2><%= I18n.t('search_alerts.mailer.confirmation.title') %></h2>
    
    <p><%= I18n.t('search_alerts.mailer.confirmation.body') %></p>
    
    <div class="query-box">
      <strong><%= I18n.t('search_alerts.mailer.confirmation.query_label') %>:</strong>
      "<%= @alert.query %>"
    </div>
    
    <p style="text-align: center; margin: 24px 0;">
      <a href="<%= @confirm_url %>" class="button">
        <%= I18n.t('search_alerts.mailer.confirmation.button') %>
      </a>
    </p>
    
    <p style="font-size: 14px; color: #6b7280;">
      <%= I18n.t('search_alerts.mailer.confirmation.link_fallback') %><br>
      <a href="<%= @confirm_url %>" style="word-break: break-all;"><%= @confirm_url %></a>
    </p>
    
    <div class="footer">
      <p><%= I18n.t('search_alerts.mailer.confirmation.ignore') %></p>
    </div>
  </div>
</body>
</html>
